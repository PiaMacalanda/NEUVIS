import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function TestComp() {
    return (
        <View>
            <Text className="bg-pink-500">MY TESTCOMP</Text>
        </View>
    );
};

const style = StyleSheet.create({});